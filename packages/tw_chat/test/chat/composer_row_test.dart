// composer_row.dart previously exposed platform-specific helper functions
// (isMobileWebTextInputPlatform, composerSelectionControlsForPlatform, etc.)
// that were tested here. Those functions have been removed now that the
// composer uses SuperTextField, which handles platform differences internally.

void main() {}

