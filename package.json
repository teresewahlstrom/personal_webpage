{
  "name": "personal-webpage-flutter-deploy-shim",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check:colors": "powershell -ExecutionPolicy Bypass -File ./scripts/check-color-centralization.ps1",
    "deploy": "npm run check:colors && bash cloudflare-pages-build.sh",
    "build": "npm run check:colors && bash cloudflare-pages-build.sh"
  }
}
