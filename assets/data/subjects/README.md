## assets/data/subjects

Scratch area for subject-data experiments.

Current runtime source of truth is under `lib/subjects/` for frontend assets and the twin runtime repository configured through `TWIN_REPO_ROOT` for backend retrieval data.

Use this folder only for temporary notes or migration prep work.