

2. Medium: style split consistency gap in text field builders (still hardcoded to light + ad-hoc colors/weights), so they bypass the token/theming split.
styles.dart
styles.dart
hint_text.dart
hint_text.dart
Recommendation: introduce a brightness/context-aware variant (or pass brightness/context in), replace `Brightness.light` and direct `Colors.*` with token/style-derived values.

3. Medium: markdown layout tokens are now theme-agnostic (as requested), but duplicate typography constants from text tokens, which can drift over time.
layout_tokens.dart
layout_tokens.dart
_dark.dart
_dark.dart
Recommendation: either
1. keep them duplicated but document as intentionally decoupled, or
2. centralize shared base constants in a neutral typography-constants module used by both text-style tokens and markdown layout tokens.

4. Low: duplicated H1 title-style construction in landing page (same algorithm in two widgets), which increases maintenance overhead.
landing_page.dart
landing_page.dart
Recommendation: extract a single helper (for example private function/extension) for “landing H1 display style from body base”.

5. Low: naming inconsistency between public token names and internal style container names.
router.dart
_styles.dart
Recommendation: rename internal `cardH1Scale/cardH2Scale` to `h1Scale/h2Scale` to match `twH1Scale/twH2Scale`.

6. Low: some landing-page typography still uses section style paths while other headings now use H1 path; this is valid, but mixed intent should be explicit.
landing_page.dart
landing_page.dart
Recommendation: decide whether those are intentionally “section” semantics; if yes, add a short code comment near each use to avoid future confusion.

Assumptions:
1. Audit intentionally excluded tw_keywords per your request.
2. Focus was style architecture consistency and split correctness, not visual taste.

If you want, I can implement the top 3 cleanup items directly in one pass:
1. textfield style builders theme-aware,
2. shared typography constants for markdown layout + text tokens,
3. single landing H1 style helper.