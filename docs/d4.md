# Focus A

# Focus B
* random geometry hovering over my logo in same color as the main bgd, why?? it is not always the same positioning. I do not know if it is different shapes or the same always because the only way i currently see it is when covering the logo-picture. 

# Focus C
Bug that we solved earlier in be025423ccf5c9c321f84869e4f8bb870d38edcf: "when adding new lines of text at the bottom beyond what fits within the extended version of the textfield that the bug occurs, the new text is out of sight (I can still scroll there, but user expects to see the text which it has just typed as it is being typed, this is regardless of were within the textfield box)."

* Related refinements:
    * As new lines are added/wrapped in the text field, the scrollbar should light up as active
    * It would be more natural if it also showed entire way to bottom as new lines are added at the bottom, right now there seems to be a padding extra downwards that it could have been scrolling down to 












Actionable work remaining is now only the broader migrations from your list, not the remove-now set.

1. Complete deprecated textInputAction removal (still active)
- Why still open: deprecated fields and reads are still present and wired into behavior.
- Current hotspots:
  - tw_textfield.dart
  - tw_textfield.dart
  - android_textfield.dart
  - android_textfield.dart
  - ios_textfield.dart
  - ios_textfield.dart
  - desktop_textfield.dart
  - desktop_textfield.dart
- Action: remove deprecated constructor params and route everything through imeConfiguration.inputAction defaults.

2. Complete deprecated onRightClick removal (still active)
- Why still open: deprecated callback is still declared and invoked.
- Current hotspots:
  - desktop_textfield.dart
  - desktop_textfield.dart
  - desktop_textfield.dart
- Action: migrate to tapHandlers path and delete deprecated field forwarding.

Already done and no longer actionable from that d4 section:
- LinkAttribution.url, disableLongPressSelection, innerController, Document trio, computeTextSpan/buildTextSpan path, controller clear deprecations.
- Chat-side fix for TwReadyTextController is in place at section_coordinator.dart using clear(), which is correct for that controller API.

If you want, next step can be a single focused migration PR for item 1 first (textInputAction), then item 2 (onRightClick).