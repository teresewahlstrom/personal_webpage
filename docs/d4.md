
# Focus A
Tw_chat improvments
* Research why the scrollbar does not go further down than currently in chat list. Seems like it stops before the gradient in the composer area starts
* three dots indicator does not show on android chrome, do we have a different pipeline entirely than desktop for that? should we make more DRY?
* remove anything affecting the chat height relative to browser width - except for that once exception where the browser is so small that it should fill the browser width - currently there are some weird logic causing the chat to be both taller and less tall as i drag the width of the browser


# Focus B
* make main page use same scrollable area from primitives that tw_chat uses
* add icons similar to other chatbots below each bot message
* Position the chat furthest to the right always ?
* random geometry hovering over my logo in same color as the main bgd


# Focus C
Bug that we solved earlier in be025423ccf5c9c321f84869e4f8bb870d38edcf: "when adding new lines of text at the bottom beyond what fits within the extended version of the textfield that the bug occurs, the new text is out of sight (I can still scroll there, but user expects to see the text which it has just typed as it is being typed, this is regardless of were within the textfield box)."

* Related refinements:
    * As new lines are added/wrapped in the text field, the scrollbar should light up as active
    * It would be more natural if it also showed entire way to bottom as new lines are added at the bottom, right now there seems to be a padding extra downwards that it could have been scrolling down to 


# Focus D
Bug: Text caret is not always within the text field. So when typing I cannot see the text. I can see the text caret being very far up in the UI outside of the composer area.