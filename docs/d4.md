# Focus A

# Focus B
* random geometry hovering over my logo in same color as the main bgd, why?? it is not always the same positioning. I do not know if it is different shapes or the same always because the only way i currently see it is when covering the logo-picture. 

# Focus C
Bug that we solved earlier in be025423ccf5c9c321f84869e4f8bb870d38edcf: "when adding new lines of text at the bottom beyond what fits within the extended version of the textfield that the bug occurs, the new text is out of sight (I can still scroll there, but user expects to see the text which it has just typed as it is being typed, this is regardless of were within the textfield box)."

* Related refinements:
    * As new lines are added/wrapped in the text field, the scrollbar should light up as active
    * It would be more natural if it also showed entire way to bottom as new lines are added at the bottom, right now there seems to be a padding extra downwards that it could have been scrolling down to 
















Second pass completed with repo-wide usage checks (including ignored files), focused on tw_primitives deprecated members.

Concrete deletion candidates in tw_primitives:

1. Remove LinkAttribution.url getter
- Definition: attributions.dart
- Usage found: 0 call sites in workspace
- Migration edits:
1. Delete deprecated getter `url`.
- Impact:
1. 1 file touched.
2. No call-site edits required.

2. Remove disableLongPressSelection global flag
- Definition: mobile_documents.dart
- Usage found: 0 references in workspace
- Migration edits:
1. Delete `disableLongPressSelection`.
- Impact:
1. 1 file touched.
2. No call-site edits required.

3. Remove ImeAttributedTextEditingController.innerController
- Definition: _ime_text_editing_controller.dart
- Usage found: 0 references in workspace
- Migration edits:
1. Delete deprecated getter `innerController`.
- Impact:
1. 1 file touched.
2. No call-site edits required.

4. Remove deprecated Document API trio
- Definitions:
1. document.dart getNodeIndex
2. document.dart getNodeBefore
3. document.dart getNodeAfter
- Usage found: no workspace call sites (only declarations)
- Migration edits:
1. Remove the 3 deprecated abstract signatures.
- Impact:
1. 1 file touched.
2. If any out-of-workspace consumer exists, they must use `getNodeIndexById`, `getNodeBeforeById`, `getNodeAfterById`.

Candidates removable with small internal migrations:

5. Remove deprecated computeTextSpan path
- Deprecated API:
1. attributed_text_styles.dart `computeTextSpan`
2. attributed_text_editing_controller.dart `buildTextSpan`
- Current deprecated call sites:
1. hint_text.dart
2. attributed_text_editing_controller.dart
- Migration edits:
1. Replace `hintText.computeTextSpan(hintTextStyleBuilder)` with `hintText.computeInlineSpan(context, hintTextStyleBuilder, null)` (or explicit inline builder map).
2. Replace `text.computeTextSpan(styleBuilder)` with `text.computeInlineSpan(context, styleBuilder, inlineWidgetBuilders)` in places where context is available; otherwise create a non-inline helper and call that.
3. Delete deprecated methods after migration.
- Impact:
1. 3 to 4 files touched.

6. Remove deprecated clear() methods in text controllers
- Deprecated APIs:
1. attributed_text_editing_controller.dart
2. _ime_text_editing_controller.dart
- Deprecated internal usage:
1. _ime_text_editing_controller.dart `_realController.clear();`
- Migration edits:
1. Replace `_realController.clear();` with `_realController.clearTextAndSelection();`
2. Delete both deprecated `clear()` methods.
3. Remove ignore comment `deprecated_member_use_from_same_package`.
- Impact:
1. 2 files touched.

Not safe to remove immediately without broader API migration:

7. Deprecated textInputAction fields
- Declared in:
1. tw_textfield.dart
2. android_textfield.dart
3. ios_textfield.dart
4. desktop_textfield.dart
- Still used internally in rendering/config paths.
- Migration needed:
1. Remove constructor params.
2. Replace all `widget.textInputAction` reads with `widget.imeConfiguration?.inputAction ?? default`.
3. Update TwTextField call chain.
- Impact:
1. Multiple core textfield files.

8. Deprecated onRightClick path
- Declared in:
1. desktop_textfield.dart
2. desktop_textfield.dart
- Still wired through gesture interactor.
- Migration needed:
1. Replace right-click callback path with tapHandlers-based equivalent in constructor and forwarding layer.
2. Remove fields and forwarding calls.
- Impact:
1. Desktop textfield + gesture interactor path.
