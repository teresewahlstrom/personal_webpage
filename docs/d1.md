# SOLVED
SOLVED

PERHAPS SOLVED (will verify after deployment)
* During b35f092a154fac3aa5a0b0ad44a75b6fee37f29b, we introduced a bug where selection became impossible because it was deselected immediately.


---
# SOON SOLVED


---
# PROPOSED NEXT
* SVG company logos inside of keywords?
* When small screen, the footer text should be two rows (so that the chat button do not hide footer text)


---
# LATER
* Make the header collapse/show similar to how header is displayed in Facebook, search the web to understand how


## Colors and special effects
* Gillar varken text färg eller interactive färg i dark mode
* Måste tänka ut hover-effekten jag vill ha bättre. Kanske icon som snurrar lite och blir större/mindre

* solve the line color token mess
* Footer/header/keywords-frame-outline lines should be slightly brighter for light theme.
* The dark link color should be more vibrant

* Light theme is not on brand

* Dark theme:
    * make link colors nicer
    * make scroll thumb active color nicer
    * make bot bubble bgd color less dull

* At the bottom of a truncated message, I want a gradient shadow with the same color like that of the chat grid. But always with a transparency

## Cleanup
* after streamlining fonts across repo, remove the dependencies and font files that are not used. and potentially bundle the ones used in app

## BUGS: phone / touch
    * copy formatting issue not resolved on phone
    * scrollbar issue in text input fields on phone not resolved

## Keywords impr:
    * Make placement logic better
    * Make the logic of co-locating words better and more logic
    * Make color mean something Right now color seems mostly aesthetic. 
        Better if it encodes category:
        magenta = commercial / strategic / leverage
        cyan = systems / integration / collaboration
        charcoal = engineering / technical coreThen the composition becomes legible, not just attractive.

## DRY-ify
Göra ett shared paket:
    - återanvända tw_chat scrollbar i main app från tw_chat?
    - återanvända arrow-key-scroll i tw_chat från main app?
    - gemensam färg-config mellan tw_chat och main app?
    - annat som kan ingå i detta?
    - Make the selection/deselction logic centralised into a wrapper? Create internal package tw_flutter_basics that can be shared with other internal packages as well as Main app. Move the scrollable behavior in there as well?

---
# Terese Content Development:
## Hero text
    * Need a picture and a hero text

## Twin
    * Perhaps remove contact details from chat and / or refer to webpage and / or say that the message will get sent to the stated email.

## Projects

## Keywords
    * Keywords:
        * Lägga till context engineering 
        * Mechanical engineering är menat ihop på två rader
        * inventering av kunskap
    
    * Do a proper inventory of skills
    * The keywords should be placed based on embeddings?
    * Make the keywords clickable with real examples or other info supporting it

    * Explore My Skills
        Identify -> Clarify -> Cut waste -> Coach for Change
        Education
        Advanced Data Analysis
        Automating work and everything 
        Creating joyful work processes & UI
        Design & 3D-modeling
        3D-printing


    "Ask me 8-10 questions, one at a time, that pull out personal stories I can reuse later: failures, small wins, client moments, early career memories. After each answer, tag the theme you see (resilience, obsession, rebellion, etc). Use 'AskUserQuestion' tool."





Compacted conversation

Searched codebase for "footer header keywords frame outline border line styling", 26 results

Looking at the current implementation, the footer/header/keywords-frame outline lines are set by these color tokens in app_color_theme.dart:

| Element | Token | Current Light Value | Current Dark Value |
|---------|-------|---------------------|-------------------|
| **Header border** | `lineSubtleTertiary` | `Color(0x40394183)` | `Color(0x397199FF)` |
| **Footer border** | `lineSubtleSecondary` | `Color(0x40394183)` | `Color(0x397199FF)` |
| **Keywords frame border** | `lineSubtleSecondary` | `Color(0x40394183)` | `Color(0x397199FF)` |

These are applied via:
- _page_header.dart: `AppLineTheme.subtleTertiaryFor(brightness)`
- _page_footer.dart: `AppLineTheme.subtleSecondaryFor(brightness)`
- landing_page.dart: `AppLineTheme.subtleSecondaryFor(brightness)`

To make them "slightly brighter for light theme," you'd adjust the light-theme hex values in `_AppLightColors` to be lighter (higher RGB values).

---

Findings first:

1. Medium: one border in main app is not fully centralized through AppLineStyle
- In app_modal.dart, the modal header uses a raw BorderSide with hardcoded width 1:
  - bottom: BorderSide(color: widget.headerBorderColor, width: 1)
- The color is centralized via app_modal.dart through ModalUiConfig.headerBorderFor, but width is not sourced from AppLineTheme/AppLineStyle.

2. Low: non-border line thickness remains local (text decoration)
- In _page_footer.dart, decorationThickness is hardcoded at 1.0 for link underline.
- This is not a border, but if your intent is global line-thickness governance, this is another local thickness literal.

What is centralized correctly:
- Header border via ShellUiConfig -> AppLineTheme: _page_header.dart
- Footer border via ShellUiConfig -> AppLineTheme: _page_footer.dart
- Keywords frame border via AppLineTheme.subtleSecondaryFor: landing_page.dart, applied at landing_page.dart
- Project card border via AppLineTheme.interactiveFor: landing_page.dart
- Grid painted lines via AppLineStyle.createPaint: _grid_background.dart

Direct answer:
- Not 100 percent. Main app is mostly centralized, but modal header border width is still hardcoded in one place.